services:
  everwill:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Mount the data directory for SQLite persistence across restarts
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
